<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="person">
    <select id="queryAll" resultType="person">
        select id, name, code, unit, state, grade, estate, heating, reason from cims_person
    </select>

    <select id="queryByPage" resultType="person">
        select id, name, code, unit, state, grade, estate, heating, reason from cims_person limit #{startSize},#{pageSize}
    </select>
    <select id="queryByCount" resultType="long">
        select count(1) from cims_person
    </select>
</mapper>